/**
 * React compatible type barrel — authored from scratch, no React import.
 *
 * Re-exports all React-compatible types from the _types directory.
 * Consumers can import types from `sinwan/react-type`.
 */
 
export * from "./_types/index.ts";